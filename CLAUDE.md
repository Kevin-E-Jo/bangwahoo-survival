# 필독 — 작업 실행 전 반드시 확인

이 디렉터리는 루트 worktree, `master`(통합) 브랜치 전용이다. 전체 방법론은
[docs/METHODOLOGY.md](./docs/METHODOLOGY.md) 참고. 요약:

1. `master`에는 PR 머지로만 코드가 들어온다. 여기서 기능 코드를 직접 작성·
   커밋하지 않는다 — 기능 작업은 각 `feature/*` worktree(`방과후 서바이벌
   -auth-backend`, `-town-scene`, `-combat`)에서 한다.
2. 이 디렉터리에서 직접 커밋해도 되는 건 문서뿐이다: `PORTFOLIO.md`,
   `docs/blueprint.html`, `docs/METHODOLOGY.md`, `README.md`.
3. 여러 곳이 합의해야 하는 로직은 `src/lib/game-logic/` 하나에만 둔다.
4. 보상·점수처럼 조작 가능한 상태는 서버가 시드로 독립 재계산해서 검증한다.
5. 절차 생성은 항상 결정론적 시드 기반(`createRng`)으로만 한다.
6. 요구사항이 여러 개인 작업은 순서를 제안하고, 한 단계 끝날 때마다 확인받은
   뒤 다음 단계로 넘어간다.
7. 타입체크·린트 통과만으로 "됐다"고 하지 않는다 — 실제로 실행해서 확인한다.
8. 결정·진행 상황은 `PORTFOLIO.md`에 남긴다.
9. 사용자 확인 없이 자동 실행해도 되는 범위는 항상 좁고 명시적으로 사전
   승인받은 것만이다. 임의로 범위를 넓히지 않는다.
10. 필요한 도구가 없으면 최소한으로 직접 만든다.
11. 다른 세션과의 조율은 명시적 채널로 하고, 상대 주장은 직접 검증한다.
